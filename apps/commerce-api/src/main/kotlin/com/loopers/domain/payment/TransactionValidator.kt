package com.loopers.domain.payment

import com.loopers.interfaces.api.v1.payments.PaymentRequest

class TransactionValidator {
    fun validate(transaction: PaymentRequest.TransactionInfo, payment: Payment) {
    }
}
